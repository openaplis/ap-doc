<!doctype html>
<html>
  <head>
    <title>
      {{#if title}}
        {{title}}
      {{else}}
        Open AP LIS Report
      {{/if}}
    </title>
    <style>
      {{> style }}
    </style>
  <body>
    <div class="page">
      <div class="whiteBox" style="width: 217mm; border: none;">
        {{#> content-block}}
          {{!-- report content goes here. --}}
        {{/content-block}}
      </div>
      <div class="whiteBox" style="width: 217mm; border: none;">
        {{#> content-footer}}
          {{!-- footer goes here. --}}
        {{/content-footer}}
      </div>
    </div>
  </body>
</html>
